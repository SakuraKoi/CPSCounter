name: CpsCounter
main: ldcr.CpsCounter.CpsCounter
version: 2
author: Ldcr
depend: ["LdcrUtils"]
commands:
  cps:
    description: get other player cps
    usage: /cps
permissions:
  cpscounter.cps:
    default: op
